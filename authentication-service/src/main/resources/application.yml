spring:
   application:
      name: authentication-service

server:
   port: 8303

eureka:
   client:
      registerWithEureka: true
      fetchRegistery: true
      serviceUrl:
         defaultZone: http://localhost:8302/eureka/
   instance:
      hostname: localhost