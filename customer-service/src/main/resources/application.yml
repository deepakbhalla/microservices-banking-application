spring:
   application:
      name: customer-service

server:
   port: 8304

eureka:
   client:
      registerWithEureka: true
      fetchRegistery: true
      serviceUrl:
         defaultZone: http://localhost:8302/eureka/
   instance:
      hostname: localhost